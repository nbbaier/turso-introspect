# Implementation Plans

Generated by the improve skill on 2026-06-11 at commit `633046f`. Execute in the order below unless dependencies say otherwise.

Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan                                       | Title                                                          | Priority | Effort | Depends on                | Status |
| ------------------------------------------ | -------------------------------------------------------------- | -------- | ------ | ------------------------- | ------ |
| [001](001-test-baseline.md)                | Establish a working test baseline with bun test                | P1       | M      | —                         | DONE   |
| [002](002-packaging-and-ci.md)             | Fix TypeScript packaging and add a CI workflow                 | P2       | S      | 001                       | TODO   |
| [003](003-diff-ignore-generated-header.md) | Make `diff` ignore the generated header                        | P1       | S      | 001                       | TODO   |
| [004](004-fix-view-trigger-filtering.md)   | Make table filters treat views and triggers correctly          | P1       | M      | 001                       | TODO   |
| [005](005-logger-to-stderr.md)             | Route all status logging to stderr (`--stdout` pipe safety)    | P2       | S      | 001                       | TODO   |
| [006](006-dependency-vulnerabilities.md)   | Clear the `bun audit` vulnerability report                     | P2       | S      | 001                       | TODO   |
| [007](007-batch-pragma-introspection.md)   | Replace per-table PRAGMA round trips with set-based queries    | P2       | M      | 001, 004                  | TODO   |
| [008](008-fix-stale-docs.md)               | Correct actively-wrong claims in README/SPEC/IMPROVEMENT-IDEAS | P3       | S      | —                         | TODO   |
| [009](009-library-api-mode.md)             | Expose a programmatic library API alongside the CLI            | P3       | M      | 002 (rec: after 003, 004) | TODO   |
| [010](010-typescript-format.md)            | Add `--format typescript` output                               | P3       | M      | 001 (soft: after 009)     | TODO   |
| [011](011-migration-format-spike.md)       | Design spike: real `--diff-format migration`                   | P3       | M      | rec: after 003            | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always.** Every fix plan uses `bun test` as its verification gate; 001 creates the suite and fixes the broken `test` script.
- **004 before 007.** Both rewrite `src/lib/schema.ts`; 007's steps are written against the post-004 shape (type-aware `shouldSkip`). Executing 007 first invalidates 004's excerpts.
- **002 before 009.** Declaration emission (`dts: true`) needs `typescript` in devDependencies.
- 003/005/006/008 are mutually independent and can run in any order (or in parallel worktrees) after their deps.
- 009 should land after 003 and 004 so the public API exports the _fixed_ behavior, not the buggy one.
- 011 produces a design doc only; its implementation gets a future numbered plan after maintainer review.

## Findings considered and rejected

- **Shell completions** (IMPROVEMENT-IDEAS.md #5): cross-shell maintenance cost exceeds value at current adoption; commander has no first-class completion generation. Revisit post-1.0.
- **Duplicate diff computation in `diff.ts`** as a perf finding: negligible cost; folded into plan 003's rewrite anyway.
- **Missing `.js` extension in `formatter.ts:1` import**: cosmetic, works under bundler resolution; folded into plan 009 (which touches that area).
- **Retry wrapper retries non-transient errors** (`db.ts:193-211` + `retry.ts`): real but deliberately not planned this round per maintainer selection — auth/SQL errors burn ~3.5 s in pointless backoff and report "failed after 4 attempts". Candidate for the next round: classify errors (HTTP 4xx / SQL syntax → fail fast).
- **Minor correctness cluster**, not planned this round per maintainer selection (re-audit unnecessary; locations recorded): expression-index columns serialize as the string `"null"` in JSON (`schema.ts:116` — `PRAGMA index_info` returns NULL names for expressions); `diff.ts:57-59` swallows non-CliError and falls through to a confusing remote-connection attempt; permission errors misreported as "file not found" (`db.ts:72-77`); org/database not URL-encoded in the token API path (`db.ts:114`); hardcoded `metadata.version: "1.0.0"` (`schema.ts:204`).
